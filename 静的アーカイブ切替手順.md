# 運用フロー（Google スプレッドシート版）

投稿の保存・モデレーションは Google スプレッドシート、公開ポータルは静的JSON（`lib/letters-data.json`）で運用する。

## 仕組みの概要

- 投稿フォーム（`/`）から送られた手紙は、サービスアカウント経由で Google スプレッドシートの `letters` タブに1行追記される（`status = pending`）。
- モデレーションはスプレッドシートを直接開いて `status` 列を手編集する（`pending` → `approved` または `rejected`）。管理画面（旧 `/admin`）は廃止した。
- 公開ページ（`/letters`）はスプレッドシートに直接アクセスせず、事前に焼き込んだ `lib/letters-data.json` を読むだけ。公開日（`REVEAL_ISO`）より前は常にロックされる。

## 1. イベント後：スプレッドシートで承認

1. スプレッドシート（`SHEET_ID` で指定したファイル）の `letters` タブを開く。
2. 各行を確認し、公開してよいものは `status` を `approved` に、公開しないものは `rejected` にする。
3. `visibility` が `private` の行は、`approved` にしても公開ポータルには出ない（bake側で除外される）。

## 2. 焼き込み（ローカル）

1. ローカルの `.env.local` に以下が入っていることを確認する。
   - `GOOGLE_SERVICE_ACCOUNT_JSON`（サービスアカウント鍵のJSON文字列）
   - `SHEET_ID`（スプレッドシートID）
2. プロジェクトルート（`natsu-time-capsule/`）で以下を実行する。

   ```bash
   npm run bake
   ```

3. `lib/letters-data.json` が、`status = approved` かつ `visibility = public` の手紙だけで更新される。メール・token・visibility・statusは含まれない。

## 3. 確認

- `lib/letters-data.json` の件数が想定通りか目視確認する（スプレッドシート上の `approved` かつ `public` の件数と一致するはず）。
- 中身に個人情報や非公開扱いの手紙が混ざっていないか確認する。

## 4. コミット・push

```bash
git add -A
git commit -m "手紙データを焼き込み"
git push
```

Vercel は push を検知して自動的に再デプロイする。

## 5. 動作確認

- 公開日（`REVEAL_ISO`）より前は `/api/letters` が引き続きロックされたままであること（＝公開日まで手紙は表に出ない）。
- 公開日以降、`/letters` ページで `lib/letters-data.json` の内容が表示されること。

## 6. （任意）GitHubリポジトリをPrivateに切替

`lib/letters-data.json` に手紙本文が入るため、公開日より前にリポジトリが誰でも見える状態だと手紙が早期に読めてしまう。必要であればPrivateに切り替える。

## 7. 2027年以降の運用について

タイムカプセル開封後、次回イベント（2027年）に向けた投稿受付や通知メール送信などを再開する場合は、Google Apps Script（GAS）や Resend 等を別途検討する。本サイトのコードには含まれない。
